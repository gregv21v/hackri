import React from 'react'

const Navbar = ({ fullname, image }) => {
  return (
    <div>
      <nav className="navbar navbar-default">
        <a className="navbar-brand" href="#">DE</a>
      </nav>
    </div>
  )
}

export default Navbar
